name: Build & Deploy

on:
  push:
    branches: [main]
    paths:
      - 'LaTeX/**'
      - 'Scans/embedded/**'
      - 'Transkript/*'
      - 'merge_report.md'
      - 'Scripts/**'
      - '.github/workflows/build-pdf.yml'
      - '.github/pages/**'
  workflow_dispatch:

# Only allow one deployment at a time
concurrency:
  group: pages
  cancel-in-progress: true

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  regenerate:
    runs-on: ubuntu-latest
    outputs:
      changed: ${{ steps.commit.outputs.changed }}
    steps:
      - uses: actions/checkout@v5

      - name: Convert transcripts to LaTeX pages
        run: python3 LaTeX/md2tex.py

      - name: Regenerate schulchronik.tex from pages
        run: python3 LaTeX/generate_main.py

      - name: Regenerate Transkript.txt
        run: python3 Scripts/generate_transkript_txt.py

      - name: Validate transcript structure
        run: python3 Scripts/validate_transcripts.py
        continue-on-error: true  # Some pre-pipeline pages lack standard sections (see TODO.md)

      - name: Commit if changed
        id: commit
        run: |
          git diff --quiet LaTeX/ Transkript.txt && echo "changed=false" >> "$GITHUB_OUTPUT" && exit 0
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add LaTeX/pages/ LaTeX/schulchronik.tex Transkript.txt
          git commit -m "Auto-generate LaTeX pages, schulchronik.tex, and Transkript.txt [skip ci]"
          git push
          echo "changed=true" >> "$GITHUB_OUTPUT"

  # ── Chronik outputs (PDF for now; epub/html later) ──────────────
  build-chronik:
    needs: regenerate
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          ref: ${{ github.ref }}

      - name: Pull latest (in case regenerate committed)
        run: git pull --ff-only

      - name: Build PDF
        uses: xu-cheng/latex-action@v4
        with:
          root_file: schulchronik.tex
          working_directory: LaTeX
          latexmk_use_lualatex: true

      - name: Upload Chronik artifacts
        uses: actions/upload-artifact@v4
        with:
          name: chronik
          path: LaTeX/schulchronik.pdf

  # ── Transkriptionsbericht ───────────────────────────────────────
  build-report:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Prepare merge report for PDF
        run: |
          mkdir -p _build
          python3 Scripts/merge_report_to_pdf_md.py merge_report.md _build/transkriptionsbericht.md

      - name: Build Transkriptionsbericht PDF
        uses: docker://pandoc/extra
        with:
          args: >-
            _build/transkriptionsbericht.md
            -o transkriptionsbericht.pdf
            --template eisvogel
            --pdf-engine=lualatex

      - name: Upload report artifact
        uses: actions/upload-artifact@v4
        with:
          name: report
          path: transkriptionsbericht.pdf

  # ── Store PDFs in repo ──────────────────────────────────────────
  store-pdfs:
    needs: [build-chronik, build-report]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Download Chronik PDF
        uses: actions/download-artifact@v4
        with:
          name: chronik
          path: pdf

      - name: Download report PDF
        uses: actions/download-artifact@v4
        with:
          name: report
          path: pdf

      - name: Commit PDFs if changed
        run: |
          git add pdf/
          git diff --cached --quiet && exit 0
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git commit -m "Update generated PDFs [skip ci]"
          git pull --rebase
          git push

  # ── Assemble & deploy to GitHub Pages ───────────────────────────
  deploy:
    needs: [build-chronik, build-report]
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v5

      - name: Download Chronik artifacts
        uses: actions/download-artifact@v4
        with:
          name: chronik
          path: _site

      - name: Download report artifact
        uses: actions/download-artifact@v4
        with:
          name: report
          path: _site

      - name: Generate index.html
        run: |
          BUILD_DATE=$(date -u +"%d. %B %Y" | sed \
            -e 's/January/Januar/' \
            -e 's/February/Februar/' \
            -e 's/March/März/' \
            -e 's/April/April/' \
            -e 's/May/Mai/' \
            -e 's/June/Juni/' \
            -e 's/July/Juli/' \
            -e 's/August/August/' \
            -e 's/September/September/' \
            -e 's/October/Oktober/' \
            -e 's/November/November/' \
            -e 's/December/Dezember/')
          sed "s/{{BUILD_DATE}}/$BUILD_DATE/g" .github/pages/index.html > _site/index.html

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: _site

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
